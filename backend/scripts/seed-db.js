require('dotenv').config();
// No seed data — start fresh. Run db:init to create tables, then use the app to create workspaces, projects, sprints, and items.
console.log('🏔  Meridian: no seed data (empty seed). Use the app to create data.\n');
