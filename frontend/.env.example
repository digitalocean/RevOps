# Copy this file to .env and fill in the values
# Local development (optional; dev server proxies to backend):
# VITE_API_URL=http://localhost:4000
# Production / DigitalOcean — set to your app URL so the built frontend calls the right API:
# VITE_API_URL=https://revops-ntkll.ondigitalocean.app
